#!/bin/sh
# https://github.com/shvedes/dotfiles 
# comments generated by ChatGPT

# The `bspc monitor` command is used to create workspaces on a particular monitor.
# In this case, 10 workspaces are being created with the names "I", "II", "III", "IV", "V", "VI", "VII", "VIII", "IX", and "X".
bspc monitor -d I II III IV V VI VII VIII IX X

##### configs

# The `bspc config` command is used to set various configuration options for bspwm.
# The `window_gap` field specifies the size of the gap between windows.
# The `border_width` field specifies the size of the border around windows.
# The `pointer_motion_interval` field specifies the minimum interval, in milliseconds, between two motion notify events.
bspc config window_gap                 10
bspc config border_width               1
bspc config pointer_motion_interval    30

##### themes

# The `catppuccin` and `gruvbox` themes are Bash scripts that set the colors for bspwm.
# These themes can be found in the `~/.config/bspwm/colors/` directory.

# $HOME/.config/bspwm/colors/catppuccin
$HOME/.config/bspwm/colors/one-dark
# $HOME/.config/bspwm/colors/gruvbox-dark
# $HOME/.config/bspwm/colors/nord

##### window rules

# The `bspc rule` command is used to set rules for how certain programs should behave when opened.
# The `bspc config external_rules_command` command specifies a script that should be run to set additional window rules.
# In this case, the script being run is `"$HOME/.local/bin/bspwm/external-rules.sh"`.
bspc config external_rules_command "$HOME/.local/bin/bspwm/external-rules.sh"

# The following `bspc rule` commands specify how certain programs should behave when opened.
# The `-a` flag specifies that the rule should apply to all instances of the program.
# The `desktop` field specifies the workspace that the program should be opened on.
# The `follow` field specifies whether the focus should follow the program when it is opened.
# The `center` field specifies whether the program should be centered on the screen when it is opened.
# The `state` field specifies the initial state of the program (e.g., floating, tiled, pseudo_tiled).
# The `rectangle` field specifies the size and position of the program when it is opened.
bspc rule -a Code            desktop='^1' follow=on  center=on
bspc rule -a Spotify         desktop='^2' follow=off state=pseudo_tiled     center=on 
bspc rule -a firefox         desktop='^3' follow=off center=off
bspc rule -a TelegramDesktop desktop='^4' follow=off center=on
bspc rule -a obs             desktop='^5' follow=off state=floating         center=on
bspc rule -a Thunar          desktop='^6' follow=on                                    rectangle=1300x800+0+0
bspc rule -a mpv             desktop='^7' follow=on  state=tiled            center=on 
bspc rule -a feh             desktop='^7' follow=on  state=tiled            center=on
bspc rule -a qBittorrent     desktop='^9' follow=on  state=floating         center=on  rectangle=1200x650+0+0

##### autostart scripts
$HOME/.local/bin/bspwm/startup.sh &
